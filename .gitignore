.env.local
.env
.venv/
__pycache__/
*.py[cod]
console-recordings/

# a deployed livekit-server config with real keys, kept next to the repo
/livekit.yaml
# per-host compose tweaks (network mode, ports)
backend/docker-compose.override.yml
